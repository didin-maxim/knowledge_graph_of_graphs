# Second pass audit: current split-file `paired_variant` relations

Дата: 2026-05-05.

Область проверки: все 16 relation entries из списка пользователя с `type: paired_variant` в `data/relations/**/*.yaml`. База не редактировалась.

## Итоговая таблица

| relation id | file | classification | математическое основание | рекомендуемая правка |
|---|---|---|---|---|
| `rel-gnomes-two-cycles-odd-even-n` | `data/relations/relations.yaml` | `keep_paired_variant` | Это два режима одной задачи Турнира городов про два цикла гномов: при нечётном `n` добрый волшебник строит систему перемычек и получает гамильтонов цикл, при чётном `n` злой волшебник использует 4-классовую раскраску и блокирует гамильтонов цикл. Отличие ровно в чётности параметра, ответы противоположны. | Оставить `type: paired_variant`; можно оставить `distance: 1`. |
| `rel-state-game-2018-paired-variants` | `data/relations/relations.yaml` | `keep_paired_variant` | Это парные случаи одной игры на ориентированных дорогах: простое государство соответствует дереву и инварианту Пети, сложное государство содержит цикл и даёт выигрыш Васи через циклический/индукционный аргумент. Связь отражает естественную дихотомию дерево против графа с циклом. | Оставить `type: paired_variant`; текст связи математически уместен. |
| `rel-spbmo-1997-p27-p34-common-acquaintance-domination-variants` | `data/relations/relations.d/deep-solution-spbmo-1997-10-p34.yaml` | `keep_paired_variant` | Обе задачи имеют одну модель: граф знакомств, у любых двух вершин есть общий сосед, нужно найти малое доминирующее по соседству множество. Решения используют один и тот же разбор по малой степени и случайный выбор; различаются только масштаб и константа (`n`/треть против миллион/5000). | Оставить `type: paired_variant`. |
| `rel-spbmo-2010-11-p3-spbmo-2010-9-p5-biased-cycle-game` | `data/relations/relations.d/deep-solution-spbmo-2010-11-p3.yaml` | `duplicate_relation_to_merge` | Это обратная копия связи `rel-spbmo-2010-9-p5-spbmo-2010-11-p3-cycle-game`: те же две карточки, тот же biased maker-breaker каркас на полном графе, тот же принцип 11 угроз против 10 удалений. Две направленные записи создают дублирование одной симметричной paired-variant связи. | Слить с `rel-spbmo-2010-9-p5-spbmo-2010-11-p3-cycle-game`: оставить одну каноническую запись между этими двумя id, перенести лучшие `forward_text/backward_text/anchors`, вторую удалить или заменить ссылкой только если схема это поддерживает. |
| `rel-spbmo-2010-9-p5-spbmo-2010-11-p3-cycle-game` | `data/relations/relations.d/deep-solution-spbmo-2010-9-p5.yaml` | `duplicate_relation_to_merge` | Та же пара карточек, что в предыдущей строке, только в обратном направлении. Содержательно это корректный paired variant: 9 класс строит короткий цикл длины 11 через лучи, 11 класс строит цикл длины 75 через запасные спицы и длинный путь; общий механизм - больше финальных угроз, чем может закрыть Министерство. Проблема не в типе, а в наличии двух записей. | Слить две записи в одну; эту можно сделать канонической, потому что её текст явно сравнивает оба решения и содержит anchors с обеих сторон. |
| `rel-spbmo-2012-p4-p7-regular-acquaintance-alternating-cycle` | `data/relations/relations.d/deep-solution-spbmo-2012-8-p7.yaml` | `needs_human_review` | Задача 7 класса доказывает локальную 4-вершинную конфигурацию в любом 40-регулярном графе. Задача 8 класса просит чередующийся 22-цикл при дополнительном ограничении на число общих соседей, причём карточка сейчас помечена как no-solution/hard и `solutions[]` пуст. Без доказанного решения нельзя утверждать, что первая задача является настоящим парным вариантом или рабочей леммой для второй. | Не оставлять как уверенный `paired_variant` без ручного решения P7. До проверки решения лучше понизить до `same_motif` или пометить связь `needs_human_review`; если в доказательстве P7 реально используется локальная 4-вершинная конфигурация, тогда заменить на `prerequisite`/lemma-style связь. |
| `rel-gabriel-connectivity-planarity-paired-lemmas` | `data/relations/relations.d/gabriel-graph-lemmas.yaml` | `change_to_prerequisite_or_lemma` | Оба узла - не варианты одной задачи, а две стандартные леммы о графе Габриэля: связность через descent по расстоянию и прямолинейная планарность через пересечение рёбер и теорему Фалеса. Они используются как разные инструменты в решении USAMO 2025 P3, но одна не является параметрическим вариантом другой. | Заменить `paired_variant` на lemma/tool relation. В текущей таксономии ближе всего `same_motif` между самими леммами или, лучше, не хранить прямую paired-связь между леммами, а оставить две уже имеющиеся `prerequisite` связи от каждой леммы к USAMO 2025 P3. |
| `rel-imo-1998-c6-small-k-k5-paired-variant` | `data/relations/relations.d/imo-1998-c6-split-links.yaml` | `keep_paired_variant` | Это два режима одной исходной задачи IMO Shortlist 1998 C6: при `k <= 4` раскраска невозможна, при `k = 5` предъявляется явная пятицветная конструкция. Граница между невозможностью и конструкцией проходит именно между этими случаями. | Оставить `type: paired_variant`. |
| `rel-imo-1998-c6-k5-large-k-paired-variant` | `data/relations/relations.d/imo-1998-c6-split-links.yaml` | `keep_paired_variant` | Оба случая конструктивные в одной задаче, но используют разные конструкции: специальная пятицветная раскраска для `k=5` и 1-факторизация `K_10` для `k >= 6`. Это соседние режимы полного разбиения по `k`. | Оставить `type: paired_variant`. |
| `rel-imo-1998-c6-small-k-large-k-paired-variant` | `data/relations/relations.d/imo-1998-c6-split-links.yaml` | `keep_paired_variant` | Это крайние режимы той же задачи: невозможность для `k <= 4` и конструкция через 1-факторизацию для `k >= 6`. Связь менее близкая, чем две соседние связи через `k=5`, но всё ещё является парой режимов одной split-задачи. | Оставить `type: paired_variant`; при желании можно снизить `distance`/текстовую заметку как "non-adjacent regimes", но менять тип не нужно. |
| `rel-imo-2024-c4-two-three-attempts-pair` | `data/relations/relations.d/imo-2024.yaml` | `keep_paired_variant` | Это нижняя и верхняя части одной задачи Turbo: две попытки недостаточны по адаптивной постановке монстров, три попытки достаточны конструктивной стратегией. Такие split-карточки образуют естественную пару "lower bound / strategy". | Оставить `type: paired_variant`. |
| `rel-imo-1996-c1-divisibility-r73-paired-regimes` | `data/relations/relations.d/imo-shortlist-before-2006-review.yaml` | `keep_paired_variant` | Это два режима одной задачи о достижимости ходами с `a^2+b^2=r`: при делимости `r` на 2 или 3 работает модульный инвариант невозможности, при `r=73` есть явный путь. Один и тот же граф ходов на доске, разные значения параметра и разные ответы. | Оставить `type: paired_variant`. |
| `rel-imo-1996-c1-divisibility-r97-paired-regimes` | `data/relations/relations.d/imo-shortlist-before-2006-review.yaml` | `keep_paired_variant` | Оба случая дают невозможность в одной исходной задаче, но через разные инварианты: модуль 2/3 против полосового паритета для `r=97`. Это не ложный друг, потому что параметры и объект совпадают, а различие как раз важно для split-разбора. | Оставить `type: paired_variant`. |
| `rel-imo-1996-c1-r73-r97-paired-regimes` | `data/relations/relations.d/imo-shortlist-before-2006-review.yaml` | `keep_paired_variant` | Два конкретных режима одной задачи с противоположными ответами: для `r=73` явный путь существует, для `r=97` путь запрещён полосовым инвариантом. Это корректная парная связь между числовыми вариантами. | Оставить `type: paired_variant`. |
| `rel-mmo-2011-firms-tc-2011-programmers` | `data/relations/relations.d/mmo-local-graph-scan-2008-2025.yaml` | `keep_paired_variant` | Условия совпадают по игровой модели: две фирмы по очереди выбирают вершины, выбранное каждой фирмой множество должно оставаться связным в графе знакомств. Отличаются параметры выделенных гениев и гарантия второй фирмы: 4/3 в ММО против 11/10 в Турнире городов. Это близкие параметрические варианты, не точный дубль и не просто общий мотив. | Оставить `type: paired_variant`; полезно добавить в текст предупреждение, что перенос решения не прямой из-за разных конструкций и параметров. |
| `rel-egmo-2025-p5-odd-even-paired-variant` | `data/relations/relations.d/solution-repair-official-links.yaml` | `keep_paired_variant` | Это нечётный и чётный случаи одной задачи EGMO 2025 P5. При нечётном `n` шахматная чётность даёт 0 хороших стартов; при чётном `n` максимум `n^2/4` достигается конструкцией динамического гамильтонова цикла и верхней оценкой. Разделение по чётности является главным split-режимом задачи. | Оставить `type: paired_variant`; можно сменить `status` с `needs_human_review` на `ai_checked` только после отдельного статусного прохода, но сам тип корректен. |

## Сводка по рекомендуемым действиям

- `keep_paired_variant`: 12 записей.
- `duplicate_relation_to_merge`: 2 записи, обе для пары СПбМО 2010 `K_2010` / `K_2009`.
- `change_to_prerequisite_or_lemma`: 1 запись, Gabriel connectivity/planarity lemmas.
- `needs_human_review`: 1 запись, СПбМО 2012 P4/P7.
- `change_to_false_friend`: 0 записей.

Главные неавтоматические места: решить, какую из двух СПбМО-2010 записей считать канонической при merge, и вручную проверить математическую роль СПбМО-2012 P4 в возможном решении P7.
