# IMO 1996 C2 plan-to-solution pass

verdict: expanded_to_full_solution

Changed files:
- `data/problems/imo/imo-1996-c2-grid-vertices-two-red.yaml`
- `audit/plan-to-solution-pass/imo-1996-c2.md`

Notes:
- Expanded the row-transition argument into an explicit classification of adjacent row pairs.
- Made the two counting cases disjoint and exhaustive: non-chessboard first row versus chessboard first row.
- Kept the solution status as `ai_checked` because the proof is now self-contained.

Tests:
- failed: `python tools/validate.py` stops while loading unrelated file `data/problems/imo/imo-1996-c1-grid-knight-reachability.yaml` with `Invalid \escape: line 114 column 549`.
- failed: `python tools/check_links.py` stops on the same unrelated JSON parse error before link checking.
- passed: direct JSON parse of `data/problems/imo/imo-1996-c2-grid-vertices-two-red.yaml`.
