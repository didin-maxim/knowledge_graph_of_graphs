# imo-2010-c5-bad-company-tournament

verdict: already_full

changed files:
- audit/plan-to-solution-pass/imo-2010-c5.md

notes:
- The target YAML already contains a full tournament double-counting solution: it defines local champions and local losers, counts them in \(k\)-subsets for \(k=2,3,4\), uses the no-bad-company condition to prove \(\sum_i \binom{w_i}{3}\ge \sum_i \binom{l_i}{3}\), and converts these counts to \(\sum_i(w_i-l_i)^3\ge0\) with the stated binomial identity.
- No edits were made to data/problems/imo/imo-2010-c5-bad-company-tournament.yaml in this pass. The file was already modified in the worktree before this pass.

tests:
- passed: python tools/validate.py
- passed: python tools/check_links.py
- passed: target file parses as JSON via ConvertFrom-Json
- passed: git diff --check for the target problem file
